extends layouts/base

block content
	p Hi #{user.name},
	p You recently requested a link to reset your #{site.name} password.
	p Please set a new password by following the link below:
	a(href=host + link)= host + link