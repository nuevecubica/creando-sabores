.row.position
	a(href="{{url}}").hover-list
	.five.wide.column
		.thumbnail(style='background-image: url({{thumb.list}})')

	.eleven.wide.column
		a(href="{{url}}")
			h2.ui.header {{title}}

		.rating
			.strong= __('Rating')
			.span
				.ui.icon {{formatIcons rating 'star'}}

		.ui.content {{{tip}}}