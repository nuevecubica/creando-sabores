mixin user-header(user)
  .ui.menu
    .right.menu
      if user
        a.ui.item.profile(href='/perfil')
          img.ui.image.avatar(src="#{user.thumb.avatar_small}")

          if user.name
            strong.username #{user.name}
          else
            strong.username #{user.username}

          nav#contextual-menu.menu
            .ui.item
              a(href='/nueva-receta')= __('Create recipe')
            .ui.item
              a(href='/perfil/compra')= __('My shopping list')
            .ui.item
              a(href='/perfil/recetas')= __('My recipes')
            .ui.item
              a(href='/perfil')= __('My profile')
            .ui.item
              a(href="/salir")= __('To logout')

        if user.canAccessKeystone
          .ui.item.admin
            a.ui.button.chef.button-brick(href="/keystone")= __('Admin panel')

        .ui.item.logout
          a(href="/salir")= __('To logout')

      else
        .ui.item
          a.ui.button.chef.button-brick(href="/acceso")= __('To login')
        .ui.item
          a.ui.button.chef.button-brick(href="/registro")= __('To register')

      #menu.ui.item.menu-icon
        i.icon-chef-menu